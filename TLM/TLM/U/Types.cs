namespace TrafficManager.U {
    public enum TransformField {
        Left,
        Top,
        Right,
        Bottom,
        Width,
        Height
    }

    public enum Unit {
        ScreenWidth,
        ScreenHeight,
        OwnWidth,
        OwnHeight,
        ParentWidth,
        ParentHeight,
        Pixels
    }
}